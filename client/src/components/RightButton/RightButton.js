import React from 'react';
import { Button } from 'antd';

const RightButton = props => {
    <Button ghost icon='right' style={{paddingBottom: '1vh'}} shape='circle' onClick={this.props.clicked}></Button>
}

export default RightButton;